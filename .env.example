# AWS Configuration
# When using AWS SSO, only region is needed here
# AWS SDK will automatically use credentials from ~/.aws/credentials
AWS_REGION=us-east-1

# Optional: Specify AWS profile if using multiple SSO profiles
# AWS_PROFILE=your-sso-profile-name